generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

model Feedback {
  id         String  @id @default(cuid())
  type       String
  comment    String
  screenshot String?

  @@map("feedbacks")
}
